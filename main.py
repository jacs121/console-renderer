import renderer
import keyboard

def changedSize(size):
    return

playerPos = (0,0)
playerRoomPos = (0,0)

def tick(size):
    global playerPos
    return

render = renderer.ConsoleRenderer(tick, changedSize)
render.run(60)